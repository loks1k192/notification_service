# Task Management API — асинхронный сервис уведомлений

> Асинхронный Task Management сервис на **FastAPI** с отдельным **Notification Service** (RabbitMQ + Redis).
> Чистая архитектура, полная async-реализация и event-driven подход.


## 📌 Кратко

Проект состоит из двух сервисов:

* **Task Management API** — REST API (FastAPI) с PostgreSQL, SQLAlchemy 2.0 и JWT-аутентификацией.
* **Async Notification Service** — асинхронный consumer RabbitMQ с Redis-кэшем для дедупликации событий.

Идея: все операции с задачами публикуют события в очередь, а отдельный сервис асинхронно обрабатывает уведомления, предотвращая дубли с помощью Redis.

---

## 🏗️ Архитектура и принципы

* **Модульный монолит** с чётким разделением ответственности.
* **Service Layer Pattern** — бизнес-логика вынесена из HTTP-роутов.
* **Async / Await** — асинхронная работа с БД, RabbitMQ и Redis.
* **Type Hints** — строгая типизация по всему проекту (mypy-friendly).
* **Мультипользовательский режим** — жёсткая изоляция данных по владельцу.

---

## 📁 Структура проекта

```
.
├── task_service/               # Основное FastAPI-приложение
│   ├── main.py                 # Точка входа приложения
│   ├── config.py               # Конфигурация
│   ├── database.py             # Подключение к БД и сессии
│   ├── models.py               # SQLAlchemy модели (User, Task)
│   ├── schemas.py              # Pydantic v2 схемы
│   ├── auth.py                 # JWT-аутентификация
│   ├── routers/                # HTTP-роуты
│   │   ├── auth.py
│   │   └── tasks.py
│   └── services/               # Слой бизнес-логики
│       ├── user_service.py
│       ├── task_service.py
│       └── event_service.py    # Публикация событий в RabbitMQ
│
├── notification_service/       # Сервис обработки событий
│   ├── consumer.py             # RabbitMQ consumer
│   ├── processor.py            # Логика обработки уведомлений
│   └── config.py               # Конфигурация
│
├── common/                     # Общие модули
│   ├── schemas/
│   │   └── events.py           # Схемы событий RabbitMQ
│   └── utils/
│       ├── config.py
│       └── rabbitmq.py
│
├── alembic/                    # Миграции БД
├── docker-compose.yml          # Оркестрация сервисов
├── Dockerfile.task             # Docker-образ Task Service
├── Dockerfile.notification     # Docker-образ Notification Service
└── requirements.txt            # Python-зависимости
```

---

## ✨ Основные возможности

### Task Management API

* **JWT-аутентификация** (access + refresh токены)
* **Мультипользовательская изоляция** данных (owner-based)
* **CRUD для задач**: создание / чтение / обновление / удаление
* **Поиск и фильтрация** по статусу и тексту
* **Пагинация** для списков задач
* **Публикация событий** в RabbitMQ при изменениях задач
* **Глобальная обработка ошибок** и единый формат ответов
* **OpenAPI / Swagger UI** (автодокументация)

### Notification Service

* **Асинхронный RabbitMQ consumer**
* **Redis-дедупликация** — защита от повторной обработки событий
* **Поддержка типов событий**: создание, обновление, удаление, смена статуса
* **Асинхронная обработка** и масштабирование consumer'ов
* **Логирование** для мониторинга и отладки

---

## 🛠️ Технологический стек

**Backend**

* FastAPI
* SQLAlchemy 2.0 (Async)
* Pydantic v2
* PostgreSQL 15+
* Alembic

**Очереди и кэш**

* RabbitMQ
* Redis

**Аутентификация**

* JWT (python-jose)
* bcrypt (passlib)

**DevOps**

* Docker & Docker Compose
* Multi-stage сборка образов

---

## 📦 Системные требования

* Docker & Docker Compose (рекомендуется)
* Python 3.11+ (для локальной разработки)
* PostgreSQL 15+ (если без Docker)
* RabbitMQ 3+ (если без Docker)
* Redis 7+ (если без Docker)

---

## 🚀 Быстрый старт

### Запуск через Docker Compose (рекомендуется)

```bash
docker-compose up -d
```

После запуска будут доступны:

* PostgreSQL — 5432
* Redis — 6379
* RabbitMQ — 5672 (UI: 15672)
* Task Service API — 8000
* Notification Service — (в логах контейнера)

**Доступы:**

* API: `http://localhost:8000`
* Swagger: `http://localhost:8000/docs`
* RabbitMQ UI: `http://localhost:15672` (guest / guest)

Остановить все контейнеры:

```bash
docker-compose down
```

---

## 🧰 Локальная разработка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` (пример):

```env
DATABASE_URL=postgresql+asyncpg://taskuser:taskpass@localhost:5432/taskdb
REDIS_URL=redis://localhost:6379/0
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
JWT_SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
```

3. Запустите миграции:

```bash
alembic upgrade head
```

4. Запуск сервисов для разработки:

* API:

```bash
uvicorn task_service.main:app --reload --port 8000
```

* Notification Service:

```bash
python -m notification_service.consumer
```

---

## 🔄 Поток событий (event flow)

1. Пользователь делает операцию с задачей (создать / обновить / удалить).
2. Task Service публикует событие в RabbitMQ.
3. Notification Service получает событие (consumer).
4. Перед обработкой проверяется Redis на наличие данного события (дедупликация).
5. Если событие новое — выполняется обработка (уведомления, логика бизнес-правил и т.д.).
6. Статус обработки сохраняется / логируется.

---

## 🗄️ Схема базы данных (кратко)

### Users

* `id` (UUID)
* `email`
* `username`
* `hashed_password`
* `is_active`
* `created_at`, `updated_at`

### Tasks

* `id` (UUID)
* `title`
* `description`
* `status`
* `owner_id` (FK -> users.id)
* `created_at`, `updated_at`

---

## 🔒 Безопасность и надежность

* JWT-токены с short-lived access / long-lived refresh.
* Пароли хранятся в виде хеша (bcrypt).
* Redis используется для дедупликации сообщений, предотвращения повторной обработки.
* Все операции с данными — асинхронные, с таймаутами и retry-механикой в местах интеграции с внешними сервисами.

---

## ✅ Качество кода и стандарты

* Полная `async`-реализация (DB, broker, cache).
* Строгая типизация с аннотациями.
* Чистая архитектура: роуты ↔️ сервисы ↔️ репозитории.
* RESTful API, детализированные ошибки, разделение обязанностей.
* Документированный код и миграции Alembic.

---

## 💡 Идеи для развития

* Refresh-token endpoint (полный flow).
* Email / Telegram / Push-уведомления как плагины.
* Rate limiting (API gateway).
* Unit + integration тесты (coverage).
* CI/CD (GitHub Actions).
* Мониторинг (Prometheus + Grafana).
* Теги, дедлайны, совместная работа над задачами.

---

## 🧩 Полезные команды и подсказки

* Создать и применить миграции:

```bash
alembic revision -m "описание" --autogenerate
alembic upgrade head
```

* Запуск тестов (пример):

```bash
pytest tests/ --maxfail=1 -q
```

* Просмотреть логи RabbitMQ контейнера:

```bash
docker-compose logs -f rabbitmq
```

---

## 📄 Лицензия

Проект предназначен для демонстрационных и портфолио-целей. Используйте на своё усмотрение. (Добавьте нужную лицензию, например MIT, при необходимости.)

---

## ✍️ Заметки разработчика

Проект демонстрирует:

* Service Layer архитектуру
* Асинхронный Python (async/await)
* Event-driven подход с RabbitMQ
* Redis для дедупликации
* Чистую, масштабируемую структуру и DevOps-базу (Docker)


